# == Schema Information
#
# Table name: blockchains
#
#  id                   :bigint           not null, primary key
#  name                 :string(255)
#  testnet              :boolean          default(FALSE)
#  explorer_token_url   :string(255)
#  created_at           :datetime         not null
#  updated_at           :datetime         not null
#  explorer_address_url :string(255)
#

blockchain_one:
  name: Ethereum
  testnet: false
  explorer_token_url: "https://etherscan.io/token/{address}"
  explorer_address_url: "https://etherscan.io/token/{address}"

blockchain_two:
  name: Pangolin
  testnet: true
